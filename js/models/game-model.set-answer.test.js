import {assert} from 'chai';
import GameModel from "./game-model";

describe(`Модель игры`, () => {

it(`setAnswer(answer) сохранил ответ и переключил уровень`)
  const gameModel = new GameModel();
  gameModel._state.currentLevel = 0;
});
